import 'package:flutter/material.dart';

class DetailsCharactersScreen extends StatelessWidget {
  const DetailsCharactersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Details Characters Screen')),
    );
  }
}
